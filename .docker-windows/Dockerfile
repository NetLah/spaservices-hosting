ARG ASPNET_IMAGE_TAG=6.0-nanoserver-1809
ARG ASPNET_IMAGE_REPO=mcr.microsoft.com/dotnet/aspnet
ARG ASPNET_IMAGE=${ASPNET_IMAGE_REPO}:${ASPNET_IMAGE_TAG}
FROM ${ASPNET_IMAGE}
WORKDIR /app
EXPOSE 80
COPY app .
ENTRYPOINT ["dotnet", "WebApp.dll"]
